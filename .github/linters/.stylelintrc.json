{
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["tailwind", "apply", "layer", "screen"]
      }
    ]
  },
  "overrides": [
    {
      "files": ["**/tailwind.input.css"],
      "rules": {
        "selector-id-pattern": null
      }
    }
  ]
}
